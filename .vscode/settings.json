{
    // a language server that enables Bazel build files navigation support
    "bazel.lsp.command": "starpls",
    "clangd.arguments": [
        "--header-insertion=never",
        "--compile-commands-dir=${workspaceFolder}/",
        "--query-driver=**"
    ]
}